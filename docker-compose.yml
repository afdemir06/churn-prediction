version: '3.8'
services:
  api:
    build: .
    command: uvicorn api:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
  streamlit:
    build: .
    command: streamlit run app.py --server.port 8501
    volumes:
      - .:/app
    ports:
      - "8501:8501"